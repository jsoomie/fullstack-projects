import {
  REGISTER_SUCCESS,
  REGISTER_FAIL,
  USER_LOADED,
  AUTH_ERROR,
  LOGIN_FAIL,
  LOGOUT,
  CLEAR_ERRORS,
} from "../actions";

export const authReducer = (state: any, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};
